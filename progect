import sys
import math

number=     #number는 규범님께서 담당하신 부분에서 구하신 숫자입니다. 
def get_prime(n):
    is_prime=[]
    if n==2:
        is_prime.append(n)
	return -1
    max_length=int(math.sqrt(n))+2
    
    for i in range(2,max_length):
        if n%i!=0 and i==max_length-1:
	    is_prime.append(n)
	    


#아직 미완성입니다. 나머지는 좀 더 공부해봐야겟네요. 실행은 제대로 될련지..?
